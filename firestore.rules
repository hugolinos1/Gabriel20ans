rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to submit an RSVP
    match /rsvps/{rsvpId} {
      allow create: if true;
      // Only authenticated users can read the RSVPs
      allow read, update, delete: if request.auth != null;
    }
  }
}
